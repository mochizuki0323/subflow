/** User-visible product name (window title, installer, package metadata). */
export const PRODUCT_NAME = 'SubFlow';

/** Where releases are published; the update check reads this repo's latest one. */
export const GITHUB_REPO = 'mochizuki0323/subflow';
